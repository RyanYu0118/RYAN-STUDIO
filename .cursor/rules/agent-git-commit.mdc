---
description: Agent 提交/推送与 GitHub Release 的询问规范；提交以开发改动为主，运行时噪声次要带过
alwaysApply: true
---

# Agent 主导的 Git 提交规范

本仓库 Minecraft 服务器**后台持续运行**，会产生大量与开发并行的变更（物价、玩家数据、区块、世界状态等）。

## 何时提交

- 完成一轮**代码/配置开发**后，**默认直接** `git commit` + `git push`，**无需再问**（用户偏好：默认 y）。
- 仅当用户在本轮对话中说过 **`n` / `跳过`** 才暂不提交；说 **`c` / `只提交`** 则只 commit 不 push。
- 大范围、含敏感文件或用户明确说「先别提交」时，仍可用 **AskQuestion** 确认。
- 单行简写（不区分大小写）：
  - `y` / `yes` / `提交并推送` → 提交并推送（与默认行为相同）
  - `c` / `commit` / `只提交` → 仅 `git commit`
  - `n` / `no` / `跳过` → 跳过提交
  - **`r` / `release` / `发版` / `GitHub Release` → 隐含 `y`：先提交并推送（若有未提交改动），再打 GitHub Release**

## 暂存范围

提交时分两层处理：

### 1. 核心改动（优先描述）

本会话中 Agent 或用户**有意修改**的路径，例如：

- `tools/**` 源码与 build 脚本
- `plugins/Skript/scripts/**`
- 自研插件源码与 `plugin.yml`（`tools/mcwws-*`、`MCWWS_*`）
- 用户点名要纳入的配置

### 2. 运行时噪声（一并推送，提交说明次要带过）

**默认与核心改动一同暂存并推送**，不在正文中逐文件展开，只在列表**末尾用一条**概括，例如「同步服务器运行时状态与物价数据」。

常见噪声文件包括但不限于：

- `level.dat`、`level.dat_old`、`raids.dat`、`raids_end.dat`
- `**/*.mca`
- `plugins/**/market_state.yml`、`price_history.csv`、`web_prices.yml`
- 玩家数据、Essentials userdata、`usercache.json`
- 插件数据库/日志/缓存、`.paper-remapped/**`
- 其他明显由服务器运行时写入的已跟踪文件

推荐命令：

1. 先 `git add` 本会话核心路径（明确可控）
2. 再 **`git add -A`**（仓库根目录）纳入**全部**已跟踪变更与**未跟踪**的运行时/构建产物（Slimefun 备份与 error-reports、`tools/**/build/` 等）；**不要**仅用 `git add -u`（会漏掉 `??` 未跟踪文件）
3. 提交前 **`git status`** 应无剩余改动；若仍有 `??` 且非敏感文件，继续 `git add` 直至干净
4. **不要** `git add` 未跟踪的敏感文件（`.env`、密钥、凭证）；若出现应先警告用户

**禁止**仅凭 `git status` 统计或文件修改时间**代替**对核心改动的意图理解来撰写标题与正文主干。

## 提交信息怎么写

1. **标题与正文前几条以本会话实际完成的开发内容为准**（Agent 记忆中的改动意图）。
2. 遵循根目录 `.cursorrules` 的**双语 Conventional Commits** 格式（中文标题 + 英文标题；复杂变更附中英列表正文）。
3. 正文**先写**核心逻辑变更（做了什么、为什么），**不要**在正文前几项罗列噪声文件名。
4. 若有噪声文件一并提交，在中文列表与英文列表的**各自最后一条**用一句话带过，例如：
   - `- 同步服务器运行时状态、物价与市场数据等`
   - `- Synced server runtime state, prices, and market data`

## 推荐流程

1. `git status` / `git diff -- <核心路径>` 核对开发改动
2. `git add` 核心路径，再 `git add -A` 纳入噪声与未跟踪文件；`git status` 确认工作区干净
3. 用 HEREDOC 写入双语 commit message（噪声放末尾一条）
4. `git commit` 后 `git status` 验证
5. **默认** `git push`；用户本轮说过 `c` 则跳过 push

## GitHub Release（RS_WikiLink 等带 tag 工作流）

完成一轮**插件版本发布**（已 commit + push，且 `gradle.properties` / `plugin.yaml` 版本号已 bump）后，**必须用 AskQuestion** 询问是否打 GitHub Release（与提交询问同级，不要静默打 tag）。

**说明：** Cursor 聊天无法提供常驻自定义按钮；AskQuestion 选项卡即「可点击按钮」。用户随时发 `r` 即可触发 **提交推送 + Release**（`r` 默认含 `y`），无需打完整句子。

### 何时询问

- `tools/halo-plugin-wikilink/` 版本号升级后（开发完成、尚未 push 或尚未打 tag 时）
- 用户明确要求「发版 / release / 打 tag」时可直接执行，仍可在执行前简短确认

### 用户简写（不区分大小写）

- **`r` / `release` / `发版` / `GitHub Release`** → **先 `y`（commit + push，若有未提交改动）**，再 build、打 tag 并 push tag，触发 Actions 发布
- **`n` / `no` / `跳过` / `暂不发布`** → 跳过 Release（不影响是否已单独执行过 `y`）

### Release 流程（RS_WikiLink）

1. **若工作区有改动**：按上文「推荐流程」完成 `git add` → commit → push（等同 `y`）
2. 确认 `tools/halo-plugin-wikilink/gradle.properties` 版本与本次改动一致
3. 运行 `tools/halo-plugin-wikilink/release.ps1`（build + `RS_WikiLink-v<version>` tag + push tag）
4. 告知用户 Release 页：`https://github.com/RyanYu0118/RYAN-STUDIO/releases/tag/RS_WikiLink-v<version>`

### Release 正文（只写相对上一版的变更）

- GitHub Release **仅说明本版相比上一 tag 改了什么**（2–5 条 bullet 即可）
- **禁止**每版重复：安装步骤、功能总表、`rs-config` 示例、验证命令（除非本版专门改了这些）
- CI 会从 `tools/halo-plugin-wikilink/` 的 git log **自动提取**中文 commit 标题生成 Release 正文
- 对话里向用户说明发版内容时，用**同样口径**（相对上一版的 delta），不要复读完整 README
- 写 commit 时用清晰的中文标题，便于 Release 自动文案准确

**禁止**在用户未同意（未点选项、未发 `r`）时自行 push release tag。
