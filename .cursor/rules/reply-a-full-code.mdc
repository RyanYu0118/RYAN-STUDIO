---
description: 用户回复 a 时输出当前话题的完整可粘贴代码块
alwaysApply: true
---

# 简写指令：a = 完整代码

当用户在本会话中发送 **`a`**（单独一行，或消息主体就是 `a`），视为等同：

> **完整代码发我**

## 必须执行

1. **输出完整代码**：给出当前讨论对象的全文（如 HTML/CSS/JS 整段、配置文件、脚本等），不要只给 diff 或片段。
2. **可直接粘贴**：代码块内应是用户能整段复制到 Halo 编辑器 / 文件里的成品，不要省略 `...`。
3. **沿用本会话上下文**：基于当前正在改的内容（最新版本），包含本会话已确认的正确 slug、class、属性等。
4. **简短前言即可**：最多 1～2 句说明用途或改动点，然后紧跟完整代码块；不要冗长解释代替代码。

## 不适用时

- 若当前话题没有「一整段代码」可交付（纯问答、排查结论、Git 操作），照常回答，并说明无完整代码块可发。
- 若用户消息是 `a` 但明显指别的（如选项 A、分支名），按上下文判断，不机械套本规则。

## 同义触发（可选识别，不强制）

以下也视为同一意图：`完整代码`、`发完整代码`、`完整代码发我`（用户已显式说明时优先）。
